[project]
name = "project-starttrek"
version = "0.1.0"
description = "A tiny FastAPI server exposing a Gemini chatbot endpoint"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "google-genai>=1.0",
    "python-dotenv>=1.0",
]
